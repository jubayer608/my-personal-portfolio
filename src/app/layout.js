import { Inter } from "next/font/google";
import "./globals.css";
import clsx from "clsx";
import FireFliesBackground from "@/components/FireFliesBackground";
import Sound from "@/components/Sound";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata = {
  title: {
    template: "%s | Jubayer — Frontend Developer",
    default: "Jubayer — Creative Frontend Developer | Portfolio",
  },
  description:
    "Welcome to the personal portfolio of Jubayer — a passionate Frontend Developer from Bangladesh. Specializing in React, Next.js, and Three.js to build modern, interactive, and visually stunning web experiences.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={clsx(
          inter.variable,
          "bg-background text-foreground font-inter"
        )}
      >
        {children}
        <FireFliesBackground />
        <Sound />
        <div id="my-modal" />
      </body>
    </html>
  );
}

